[project]
name = "boundary-probes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Paul Kim", email = "spkim@protonmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "inspect-ai>=0.3.263",
]

[project.scripts]
boundary-probes = "boundary_probes:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
